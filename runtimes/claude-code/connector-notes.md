# Claude Code Connector Notes

Claude Code can use direct calendar connectors where available:

- Google Calendar: read fixed events with `list_events`.
- Outlook Calendar: read fixed events with Outlook calendar search when the account is connected.

Slack is still useful for DMs, mentions, GitHub app notifications, Drive notifications, and app-level calendar alerts, but it is not required as the primary calendar read path when direct connectors work.

